'use strict';

/* Find all possible anagrams of a word in a set of words */



function findAnagram(word){
  var anagrams = {}, results = {};

  var wordsArr = words.map((word) => {
    if(anagrams.hasOwnProperty(word))
  })

  return results;

}


var testWords = ['abcd', 'adcb', 'bdca', 'aaab'];
